const divide = (x, y) => x/y
console.log(divide(10, 5))

const wisper = (text) => text.toLowerCase()
console.log(wisper("IYZU"))

const shorterThan = (x, y) => x.length < y.length ? "x lebih pendek" : "x lebih panjang"
console.log(shorterThan([1, 2, 3], [1, 2, 3, 4, 5]))